knitr::opts_chunk$set(echo = TRUE)
# Construct Medicare coverage variable (1 = covered; 0 = not covered)
mydata <- mydata %>% mutate(medicare = ifelse(agex >= 65, 1, 0))
here()
library(here)
library(dplyr)
library(ggplot2)
library(tidyverse)
library(ggrepel)
library(ggtext)
library(showtext)
library(stringr)
library(fastDummies)
library(modelsummary)
library(lubridate)
library(gtsummary)
library(fixest)
here()
mydata <- load(a3_p3_medicare.RData)
here()
mydata <- load(a3_p3_medicare.RData)
setwd("~/Documents/GitHub/HAD5744/Assignment 3")
mydata <- load(a3_p3_medicare.RData)
load("/Users/husaynjessa/Documents/GitHub/HAD5744/Assignment 3/a3_p3_medicare.RData")
load("/Users/husaynjessa/Documents/GitHub/HAD5744/Assignment 3/a3_p3_medicare.RData")
View(mydata)
# Construct Medicare coverage variable (1 = covered; 0 = not covered)
mydata <- mydata %>% mutate(medicare = ifelse(agex >= 65, 1, 0))
medicare <- mydata[mydata$medicare == 1, ]
no_medicare <- mydata[mydata$medicare == 0, ]
# View distribution of out-of-pocket medical spending for spending before Medicare coverage
ggplot(medicare, aes(x=totexp)) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 100000) + ylim(0, 20000)
# View distribution of out-of-pocket medical spending for spending after Medicare coverage
ggplot(no_medicare, aes(x=totexp)) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 25000) + ylim(0, 100000)
#distribution with log(totexp)
ggplot(medicare, aes(x=log(totexp)) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 100000) + ylim(0, 20000)
#distribution with log(totexp) after medicare coverage
ggplot(no_medicare, aes(x=log(totexp)) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 25000) + ylim(0, 100000)
# View distribution of out-of-pocket medical spending for spending before Medicare coverage
ggplot(medicare, aes(x=totexp)) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 100000) + ylim(0, 20000)
#distribution with log(totexp) before medicare coverage
ggplot(medicare, aes(x=log(totexp)) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 100000) + ylim(0, 20000)
ggplot(medicare, aes(x=totexp)) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 100000) + ylim(0, 20000)
View distribution of out-of-pocket medical spending for spending before Medicare coverage
# View distribution of out-of-pocket medical spending for spending before Medicare coverage
ggplot(medicare, aes(x=totexp) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 100000) + ylim(0, 20000)
# View distribution of out-of-pocket medical spending for spending after Medicare coverage
ggplot(no_medicare, aes(x=totexp) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 25000) + ylim(0, 100000)
# View distribution of out-of-pocket medical spending for spending before Medicare coverage
ggplot(medicare, aes(x=totexp) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 100000) + ylim(0, 20000)
# View distribution of out-of-pocket medical spending for spending after Medicare coverage
ggplot(no_medicare, aes(x=totexp) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 25000) + ylim(0, 100000)
ggplot(no_medicare, aes(x=totexp) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 25000) + ylim(0, 100000)
name <- Sys.info()
knitr::opts_chunk$set(echo = TRUE)
library(here)
library(dplyr)
library(ggplot2)
library(tidyverse)
library(ggrepel)
library(ggtext)
library(showtext)
library(stringr)
library(fastDummies)
library(modelsummary)
library(lubridate)
library(gtsummary)
library(fixest)
name <- Sys.info()
name[7]
here()
load("/Users/husaynjessa/Documents/GitHub/HAD5744/Assignment 3/a3_p3_medicare.RData")
# Construct Medicare coverage variable (1 = covered; 0 = not covered)
mydata <- mydata %>% mutate(medicare = ifelse(agex >= 65, 1, 0))
medicare <- mydata[mydata$medicare == 1, ]
no_medicare <- mydata[mydata$medicare == 0, ]
# View distribution of out-of-pocket medical spending for spending before Medicare coverage
ggplot(medicare, aes(x=totexp)) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 100000) + ylim(0, 20000)
# View distribution of out-of-pocket medical spending for spending after Medicare coverage
ggplot(no_medicare, aes(x=totexp)) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 25000) + ylim(0, 100000)
naive_ols <- lm(totexp ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt) # We will have to figure out what we want to control for
summary(naive_ols)
hist(totexp)
hist(mydata$totexp)
attach(mydata)
hist(log(totexp))
hist(totexp)
naive_ols <- lm(log(totexp) ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt) # We will have to figure out what we want to control for
naive_ols <- lm(log(totexp) ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt) # We will have to figure out what we want to control for
naive_ols <- lm(log(totexp) ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt)
hist(log(totexp))
naive_ols <- lm(log(totexp) ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt)
naive_ols <- lm(log(totexp) ~ medicare, data = mydata, weights = perwt)
naive_ols <- lm(totexp ~ medicare, data = mydata, weights = perwt)
mydata <- mydata %>% mutate(log_totexp = log(totexp))
View(mydata)
naive_ols <- lm(log_totexp ~ medicare, data = mydata, weights = perwt)
install.packages("quantreg")
library(quantreg)
quant_reg <- rq(totexp ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt)
quant_reg
quant_reg <- rq(totexp ~ medicare + cancerdx + sex + racex + faminc, tau=20 data = mydata, weights = perwt)
quant_reg <- rq(totexp ~ medicare + cancerdx + sex + racex + faminc, tau= .20 data = mydata, weights = perwt)
quant_reg <- rq(totexp ~ medicare + cancerdx + sex + racex + faminc, tau= 20, data = mydata, weights = perwt)
quant_reg <- rq(totexp ~ medicare + cancerdx + sex + racex + faminc, tau= 0.2, data = mydata, weights = perwt)
quant_reg
summary(quant_reg)
quant_reg <- rq(totexp ~ medicare + cancerdx + sex + racex + faminc, tau= 0.05, data = mydata, weights = perwt)
summary(quant_reg)
quant_reg <- rq(faminc ~ medicare, tau = 0.05, data = mydata, weights = perwt)
summary(quant_reg)
resid(quant_reg)
plot(quant_reg)
plot(quant_reg)
plot(quant_reg, mfrow =c(1,2))
plot(summary(quant_reg))
quant_reg <- rq(faminc ~ medicare + cancerdx + sex + racex, tau= c(0.05:1) , data = mydata, weights = perwt)
summary(quant_reg)
quant_reg <- rq(faminc ~ medicare + cancerdx + sex + racex, tau= c(0.05,0.1) , data = mydata, weights = perwt)
summary(quant_reg)
mydata <- mydata %>% mutate(log_totexp = log(totexp))
mydata <- mydata %>% mutate(log_totexp = log(totexp)) %>% drop_na(log_totexp)
naive_ols <- lm(log(totexp) ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt) # We will have to figure out what we want to control for
view(log_totexp)
view(mydata$log_totexp)
quant_reg <- map(quants, ~rq(faminc ~ medicare + cancerdx + sex + racex, tau= .x , data = mydata, weights = perwt)
typeof(quant_reg)
typeof(quant_reg)
summary(quant_reg)
summary(quant_reg[[20]])
summary(quant_reg[[2]])
summary(quant_reg[[3]])
summary(quant_reg[[5]])
typeof(quant_reg)
summary(quant_reg)
length(quant_reg)
quant_reg <- rq(faminc ~ medicare + cancerdx + sex + racex, tau= c(0.05, 0.1, 0.15, 0.2, 0.25, 0.3, 0.35, 0.4, 0.45, 0.5, 0.55, 0.6, 0.65, 0.7, 0.75,
0.8, 0.85, 0.9, 0.95, 0.1) , data = mydata, weights = perwt)
summary(quant_reg)
library(here)
library(dplyr)
library(ggplot2)
library(tidyverse)
library(ggrepel)
library(ggtext)
library(showtext)
library(stringr)
library(fastDummies)
library(modelsummary)
library(lubridate)
library(gtsummary)
library(fixest)
name <- Sys.info()
name[7]
assign_data <- read.csv("a3_p1_sourdough_trends.csv")
#create column new_date to change the date variable into dates
assign_data$date <- str_replace_all(assign_data$date, "T00:00:00Z", "")
assign_data$new_date <- ymd(assign_data$date)
class(assign_data$new_date)
hq1_line_graph <- ggplot(assign_data, aes(x = date, y = hits, colour = keyword, group = keyword)) +
theme_classic()  + geom_vline(xintercept = assign_data$date[75], show.legend = FALSE) + annotate("text", x=assign_data$date[70], y=86, label="Start of Pandemic ", angle=90) + geom_line() +  scale_x_discrete(breaks = c("2020-01-01",
"2020-02-01",
"2020-03-01",
"2020-04-01",
"2020-05-01",
"2020-06-01",
"2020-07-01",
"2020-08-01")) + ggtitle("Interest in Food Types Over Time")
hq1_line_graph
# Look at plot at pre-treatment, to gauge trend lines and similarities or differences:
hq1_line_graph + coord_cartesian(xlim= c(0,76))
#we can see before the start of the pandemic sourdough looked similar to cereal but did not look anything like soup or sandwich so considering droping thos
# Create Treated Variable for sourdough https://www.princeton.edu/~otorres/DID101R.pdf
regdata <- assign_data %>% mutate(treated = ifelse(keyword == "sourdough", 1, 0))
# Create a dummy variable to indicate the time when the treatment started.
regdata <- regdata %>% mutate(pandemic_begun = ifelse(date >= "2020-03-15", 1, 0))
# Create interaction variable between treated and beginning of pandemic
regdata <- regdata %>% mutate(interaction = treated*pandemic_begun)
# Statistical tests: are the two groups' trends different?
pretrend_test <- lm(hits ~ treated + pandemic_begun + interaction, data=regdata)
msummary(list(pretrend_test),
vcov=c(rep("robust",1)),
stars=c('*' = .1, '**' = .05, '***' = .01)) # Interpret each coefficient here
# Dropping Control Groups
# Dropping soup and sandwich from data
new_regdata <- regdata %>% filter(keyword %in% c("sourdough","cereal"))
unique(new_regdata$keyword)
new_pretrend_test <- lm(hits ~ treated + pandemic_begun + interaction, data=new_regdata)
msummary(list(pretrend_test, new_pretrend_test),
vcov=c(rep("robust",1)),
stars=c('*' = .1, '**' = .05, '***' = .01)) # Interpret each coefficient here
#i think this is how you do it but im not sure
#create a relative_month variable by shifting the "date" variable back 15 days (so that the treatment day is the first day of the month)  and then taking the month of the resulting date
new_regdata <- new_regdata %>% mutate(relative_month = new_date - 14)
#shift date back 15 days, used new_date instead of date as date is a character while new date is the class date
# I used 14 so that the treatment day is may first rather than February 29
#create an "After" variable equal to 1/0 or True/False if the date is March 15 or afterwards
new_regdata <- new_regdata %>% mutate(After = ifelse(relative_month >= "2020-03-15", 1, 0 ))
#illustrate how the values of "relative_month" line up with "date" and subtract a number from "relative_month" so the last period just before treatment (Feb 16 - Mar 14)  is 0. (Also, change the Jan 1 -14 month so it's one less than the Jan 15- Feb 14 month)
relative_month_date_illustration <- new_regdata %>% select(relative_month, new_date)
#table with relative month and new date next to each other
relative_month_date_illustration <- relative_month_date_illustration %>% mutate(diff_bt_date = difftime(new_date, relative_month, units = "days"))
plot(relative_month_date_illustration$diff_bt_date)
q_c_table <- tibble(relative_month_date_illustration)
#Use two-way fixed effects to estimate the difference-in-difference estimate of the effect of lockdown on sourdough popularity. What are your two fixed effects?
fixed_effects_model <- feols(hits ~ treated | pandemic_begun + interaction, data = new_regdata)
#idk what fixed effects we have to include here also not sure if we include all 3 of these variables in the ols  (maybe include keyword as the fixed effect but that introduces colineaarity with treated)
fixed_effects_model
#report and interper results, with standard errors clustered at keyword level
msummary(fixed_effects_model, vcov= ~keyword, fmt = '%#.8f') #is this how you cluster by keyword level haha
knitr::opts_chunk$set(echo = TRUE)
mytau <- rep(NA,20) # empty vector: quantiles
coefs <- rep(NA, 20) # empty vector: coefficients
lb <- rep(NA, 20) # empty vector: 95% LB
ub <- rep(NA, 20) # empty vector: 95% UB
for (t in 1:20) {
mytau[t] <- t/20 # indicate which decile I am using
print(paste0("Considering quantile ",mytau[t],sep=" "))
library(here)
library(dplyr)
library(ggplot2)
library(tidyverse)
library(ggrepel)
library(ggtext)
library(showtext)
library(stringr)
library(fastDummies)
library(modelsummary)
library(lubridate)
library(gtsummary)
library(fixest)
library(here)
library(dplyr)
library(ggplot2)
library(tidyverse)
library(ggrepel)
library(ggtext)
library(showtext)
library(stringr)
library(fastDummies)
library(modelsummary)
library(lubridate)
library(gtsummary)
library(fixest)
library(quantreg)
quantreg <- rq(totexp ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt,
tau = mytau[t]) # tau ranges from 0 to 1
# Construct Medicare coverage variable (1 = covered; 0 = not covered)
mydata <- mydata %>% mutate(medicare = ifelse(agex >= 65, 1, 0))
name <- Sys.info()
name[7]
here()
load("/Users/husaynjessa/Documents/GitHub/HAD5744/Assignment 3/a3_p3_medicare.RData")
# Construct Medicare coverage variable (1 = covered; 0 = not covered)
mydata <- mydata %>% mutate(medicare = ifelse(agex >= 65, 1, 0))
medicare <- mydata[mydata$medicare == 1, ]
no_medicare <- mydata[mydata$medicare == 0, ]
# View distribution of out-of-pocket medical spending for spending after Medicare coverage
ggplot(medicare, aes(x=totexp)) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 100000) + ylim(0, 20000)
# View distribution of out-of-pocket medical spending for spending before Medicare coverage
ggplot(no_medicare, aes(x=totexp)) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 25000) + ylim(0, 100000)
quantreg <- rq(totexp ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt,
tau = mytau[t]) # tau ranges from 0 to 1
print(paste0("Considering quantile ",mytau[t],sep=" "))
mytau <- rep(NA,20) # empty vector: quantiles
coefs <- rep(NA, 20) # empty vector: coefficients
lb <- rep(NA, 20) # empty vector: 95% LB
ub <- rep(NA, 20) # empty vector: 95% UB
for (t in 1:20) {
mytau[t] <- t/20 # indicate which decile I am using
print(paste0("Considering quantile ",mytau[t],sep=" "))
quantreg <- rq(totexp ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt,
tau = mytau[t])
coefs[t] <- quantreg$coefficients[2]
mysum <- summary(myreg)
lb[t] <- coefs[t]-1.96*mysum$coefficients[2,2]
ub[t] <- coefs[t]+1.96*mysum$coefficients[2,2]
mysum <- summary(quant_reg)
lb[t] <- coefs[t]-1.96*mysum$coefficients[2,2]
ub[t] <- coefs[t]+1.96*mysum$coefficients[2,2]
mytau <- rep(NA,20) # empty vector: quantiles
coefs <- rep(NA, 20) # empty vector: coefficients
lb <- rep(NA, 20) # empty vector: 95% LB
ub <- rep(NA, 20) # empty vector: 95% UB
for (t in 1:20) {
mytau[t] <- t/20 # indicate which decile I am using
print(paste0("Considering quantile ",mytau[t],sep=" "))
quantreg <- rq(totexp ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt,
tau = mytau[t]) # tau ranges from 0 to 1
coefs[t] <- quantreg$coefficients[2]
mysum <- summary(quant_reg)
lb[t] <- coefs[t]-1.96*mysum$coefficients[2,2]
ub[t] <- coefs[t]+1.96*mysum$coefficients[2,2]
}
quantreg
for (t in 1:20) {
mytau[t] <- t/20 # indicate which decile I am using
print(paste0("Considering quantile ",mytau[t],sep=" "))
quantreg <- rq(totexp ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt,
tau = mytau[t]) # tau ranges from 0 to 1
coefs[t] <- quantreg$coefficients[2]
mysum <- summary(quant_reg)
lb[t] <- coefs[t]-1.96*mysum$coefficients[2,2]
ub[t] <- coefs[t]+1.96*mysum$coefficients[2,2]
}
quantreg
msummary(quantreg)
library(quantreg)
quant_reg <- rq(faminc ~ medicare + cancerdx + sex + racex, tau= c(0.05, 0.1, 0.15, 0.2, 0.25, 0.3, 0.35, 0.4, 0.45, 0.5, 0.55, 0.6, 0.65, 0.7, 0.75,
0.8, 0.85, 0.9, 0.95, 0.1) , data = mydata, weights = perwt)
summary(quant_reg)
knitr::opts_chunk$set(echo = TRUE)
library(here)
library(dplyr)
library(ggplot2)
library(tidyverse)
library(ggrepel)
library(ggtext)
library(showtext)
library(stringr)
library(fastDummies)
library(modelsummary)
library(lubridate)
library(gtsummary)
library(fixest)
library(quantreg)
name <- Sys.info()
name[7]
here()
load("/Users/husaynjessa/Documents/GitHub/HAD5744/Assignment 3/a3_p3_medicare.RData")
# Construct Medicare coverage variable (1 = covered; 0 = not covered)
mydata <- mydata %>% mutate(medicare = ifelse(agex >= 65, 1, 0))
medicare <- mydata[mydata$medicare == 1, ]
no_medicare <- mydata[mydata$medicare == 0, ]
# View distribution of out-of-pocket medical spending for spending after Medicare coverage
ggplot(medicare, aes(x=totexp)) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 100000) + ylim(0, 20000)
# View distribution of out-of-pocket medical spending for spending before Medicare coverage
ggplot(no_medicare, aes(x=totexp)) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 25000) + ylim(0, 100000)
naive_ols <- lm(totexp ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt) # We will have to figure out what we want to control for
summary(naive_ols)
attach(mydata)
hist(totexp)
hist(log(totexp))
#based on the histograms we decide to log totexp
view(mydata$log_totexp)
naive_ols <- lm(log(totexp) ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt) # We will have to figure out what we want to control for
library(quantreg)
quant_reg <- rq(faminc ~ medicare + cancerdx + sex + racex, tau= c(0.05, 0.1, 0.15, 0.2, 0.25, 0.3, 0.35, 0.4, 0.45, 0.5, 0.55, 0.6, 0.65, 0.7, 0.75,
0.8, 0.85, 0.9, 0.95, 0.1) , data = mydata, weights = perwt)
summary(quant_reg)
quant_reg <- rq(totexp ~ medicare + cancerdx + sex + racex, tau= c(0.05, 0.1, 0.15, 0.2, 0.25, 0.3, 0.35, 0.4, 0.45, 0.5, 0.55, 0.6, 0.65, 0.7, 0.75,
0.8, 0.85, 0.9, 0.95, 0.1) , data = mydata, weights = perwt)
summary(quant_reg)
mytau <- rep(NA,20) # empty vector: quantiles
coefs <- rep(NA, 20) # empty vector: coefficients
lb <- rep(NA, 20) # empty vector: 95% LB
ub <- rep(NA, 20) # empty vector: 95% UB
for (t in 1:20) {
mytau[t] <- t/20 # indicate which decile I am using
print(paste0("Considering quantile ",mytau[t],sep=" "))
quantreg <- rq(totexp ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt,
tau = mytau[t]) # tau ranges from 0 to 1
coefs[t] <- quantreg$coefficients[2]
mysum <- summary(quant_reg)
lb[t] <- coefs[t]-1.96*mysum$coefficients[2,2]
ub[t] <- coefs[t]+1.96*mysum$coefficients[2,2]
}
for (t in 1:20) {
mytau[t] <- t/20 # indicate which decile I am using
print(paste0("Considering quantile ",mytau[t],sep=" "))
quantreg <- rq(totexp ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt,
tau = mytau[t]) # tau ranges from 0 to 1
coefs[t] <- quantreg$coefficients[2]
mysum <- summary(quant_reg)
lb[t] <- coefs[t]-1.96*mysum$coefficients[2,2]
ub[t] <- coefs[t]+1.96*mysum$coefficients[2,2]
}
mytau <- rep(NA,20) # empty vector: quantiles
coefs <- rep(NA, 20) # empty vector: coefficients
lb <- rep(NA, 20) # empty vector: 95% LB
ub <- rep(NA, 20) # empty vector: 95% UB
for (t in 1:20) {
mytau[t] <- t/20 # indicate which decile I am using
print(paste0("Considering quantile ",mytau[t],sep=" "))
quantreg <- rq(totexp ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt,
tau = mytau[t]) # tau ranges from 0 to 1
coefs[t] <- quantreg$coefficients[2]
}
msummary(quantreg)
summary(quantreg)
for (t in 1:20) {
mytau[t] <- t/20 # indicate which decile I am using
print(paste0("Considering quantile ",mytau[t],sep=" "))
quantreg <- rq(totexp ~ medicare + cancerdx + sex + racex + faminc, data = mydata, weights = perwt,
tau = mytau[t]) # tau ranges from 0 to 1
coefs[t] <- quantreg$coefficients[2]
quantsummary <- summary(quantreg)
}
knitr::opts_chunk$set(echo = TRUE)
library(here)
library(dplyr)
library(ggplot2)
library(tidyverse)
library(ggrepel)
library(ggtext)
library(showtext)
library(stringr)
library(fastDummies)
library(modelsummary)
library(lubridate)
library(gtsummary)
library(fixest)
library(quantreg)
install.packages("quantreg")
library(quantreg)
name <- Sys.info()
name[7]
assign_data <- read.csv("a3_p1_sourdough_trends.csv")
# Create Treated Variable for sourdough https://www.princeton.edu/~otorres/DID101R.pdf
regdata <- assign_data %>% mutate(treated = ifelse(keyword == "sourdough", 1, 0))
# Create a dummy variable to indicate the time when the treatment started.
regdata <- regdata %>% mutate(pandemic_begun = ifelse(date >= "2020-03-15", 1, 0))
# Create interaction variable between treated and beginning of pandemic
regdata <- regdata %>% mutate(interaction = treated*pandemic_begun)
# Statistical tests: are the two groups' trends different?
pretrend_test <- lm(hits ~ treated + pandemic_begun + interaction, data=regdata)
msummary(list(pretrend_test),
vcov=c(rep("robust",1)),
stars=c('*' = .1, '**' = .05, '***' = .01)) # Interpret each coefficient here
# Dropping Control Groups
# Dropping soup and sandwich from data
new_regdata <- regdata %>% filter(keyword %in% c("sourdough","cereal"))
unique(new_regdata$keyword)
new_pretrend_test <- lm(hits ~ treated + pandemic_begun + interaction, data=new_regdata)
msummary(list(pretrend_test, new_pretrend_test),
vcov=c(rep("robust",1)),
stars=c('*' = .1, '**' = .05, '***' = .01)) # Interpret each coefficient here
#i think this is how you do it but im not sure
# Construct Medicare coverage variable (1 = covered; 0 = not covered)
mydata <- mydata %>% mutate(medicare = ifelse(agex >= 65, 1, 0))
name <- Sys.info()
name[7]
here()
load("/Users/aidanbodner/Documents/GitHub/HAD5744/Assignment 3/a3_p3_medicare.RData")
# Construct Medicare coverage variable (1 = covered; 0 = not covered)
mydata <- mydata %>% mutate(medicare = ifelse(agex >= 65, 1, 0))
medicare <- mydata[mydata$medicare == 1, ]
no_medicare <- mydata[mydata$medicare == 0, ]
# View distribution of out-of-pocket medical spending for spending after Medicare coverage
ggplot(medicare, aes(x=totexp)) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 100000) + ylim(0, 20000)
# View distribution of out-of-pocket medical spending for spending before Medicare coverage
ggplot(no_medicare, aes(x=totexp)) + geom_histogram() + geom_histogram(bins = 100) + xlim(0, 25000) + ylim(0, 100000)
hist(totexp)
attach(mydata)
hist(totexp)
hist(asinh(totexp))
hist(totexp^2)
hist(asinh(totexp))
hist(log(totexp+1))
hist(log(totexp)+1)
naive_ols <- lm(totexp ~ medicare + sex + educyr + racex + faminc, data = mydata, weights = perwt) # We will have to figure out what we want to control for
summary(naive_ols)
attach(mydata)
hist(totexp)
hist(log(totexp)*1)
#based on the histograms we decide to log totexp
hist(log(totexp))
naive_ols <- lm(log(totexp) ~ medicare + sex + educyr + racex + faminc, data = mydata, weights = perwt)
mytau <- rep(NA,20) # empty vector: quantiles
coefs <- rep(NA, 20) # empty vector: coefficients
lb <- rep(NA, 20) # empty vector: 95% LB
ub <- rep(NA, 20) # empty vector: 95% UB
for (t in 1:20) {
mytau[t] <- t/20 # indicate which decile I am using
print(paste0("Considering quantile ",mytau[t],sep=" "))
quantreg <- rq(totexp ~ medicare + educyear + sex + racex + faminc, data = mydata, weights = perwt,
tau = mytau[t]) # tau ranges from 0 to 1
coefs[t] <- quantreg$coefficients[2]
quantsummary <- summary(quantreg)
}
for (t in 1:20) {
mytau[t] <- t/20 # indicate which decile I am using
print(paste0("Considering quantile ",mytau[t],sep=" "))
quantreg <- rq(totexp ~ medicare + educyr + sex + racex + faminc, data = mydata, weights = perwt,
tau = mytau[t]) # tau ranges from 0 to 1
coefs[t] <- quantreg$coefficients[2]
quantsummary <- summary(quantreg)
}
knitr::opts_chunk$set(echo = TRUE)
name <- Sys.info()
name[7]
here()
library(here)
library(dplyr)
library(ggplot2)
library(tidyverse)
library(ggrepel)
library(ggtext)
library(showtext)
library(stringr)
library(fastDummies)
library(modelsummary)
library(lubridate)
library(gtsummary)
library(fixest)
library(quantreg)
# Construct Medicare coverage variable (1 = covered; 0 = not covered)
mydata <- mydata %>% mutate(medicare = ifelse(agex >= 65, 1, 0))
name <- Sys.info()
name[7]
here()
load("/Users/aidanbodner/Documents/GitHub/HAD5744/Assignment 3/a3_p3_medicare.RData")
